# Copy this file to .env for local development only.
# Do not commit your real ESV API token. Use Vercel env vars or GitHub Actions secrets for deployment.
ESV_API_TOKEN=replace_with_your_private_esv_api_token

# Optional: comma-separated origins allowed to call a separately hosted lookup proxy.
# Leave blank for same-origin Vercel hosting.
ALLOWED_ORIGINS=
